# Durgeshpatta
def print_banner(message):
    banner = f"""
    # {'#' * (len(message) + 2)} #
    # {message} #
    # {'#' * (len(message) + 2)} #
    """
    print(banner)

# Example usage:
print_banner("Hello, ChatGPT!")
